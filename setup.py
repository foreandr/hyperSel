from setuptools import setup, find_packages

setup(
    name="hyperSel",
    version=1.1,
    packages=find_packages(),
    install_requires=[
        # selenium
        # seleinum web manages
    ]
)

